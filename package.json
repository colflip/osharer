{
    "name": "osharer",
    "version": "0.1.0",
    "private": true,
    "description": "Web-based screen sharing built on Agora RTC. Open HTML files directly — no build required.",
    "scripts": {
        "serve": "python3 -m http.server 4173",
        "check": "node --check scripts/write-version.js && node --check assets/js/config-loader.js && node --check assets/js/agora-loader.js && node --check assets/js/viewer.js && node --check assets/js/sharer.js"
    }
}
